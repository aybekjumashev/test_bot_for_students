from django.urls import path
# from . import views # Keyinroq API viewlar qo'shiladi

app_name = 'tgbot'
urlpatterns = [
    # path('register/', views.RegisterTelegramUserView.as_view(), name='tg_register_user'),
]